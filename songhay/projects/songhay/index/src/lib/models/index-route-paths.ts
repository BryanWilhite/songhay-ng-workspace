/**
 * routing path data
 * for the @type {IndexModule}
 *
 * @export
 */
export enum IndexRoutePaths {
    /**
     * the root path to the components
     * in this library
     */
    root = 'index/:style'
}
